import React from 'react';


export const friendShow = props => {

  const { friendId, curUser, removeFriend } = this.props;

  return (
    <li className ="friend-name" key={key}>👤 {name}
      <span className='delete-friend-btn' onClick={() => removeFriend(friendId)}>X</span>
    </li>
  )





}

